import "server-only";
import type { ProviderResult } from "./types";
import { providerResult } from "./types";
import { providerFetchPolicy, type ProviderAcquisitionMode } from "./provider-fetch-policy";
const COINGECKO_BASE = "https://api.coingecko.com/api/v3";
const ASSET_IDS = { BTC: "bitcoin", ETH: "ethereum" } as const;
type CoinGeckoSimplePrice = { [id: string]: { usd?: number; usd_market_cap?: number; usd_24h_vol?: number; usd_24h_change?: number; last_updated_at?: number } };
type CoinGeckoGlobal = { data?: { total_market_cap?: { usd?: number }; total_volume?: { usd?: number }; market_cap_percentage?: { btc?: number; eth?: number }; updated_at?: number } };
export type CryptoMarketObservationInput = { metricId: string; symbol: string; value: number; observedAt: string; retrievedAt: string; source: string; metadata: Record<string, string | number | boolean | null> };
function observedAtFromUnix(seconds: number | undefined): string | null { if (!Number.isFinite(seconds)) return null; const date = new Date(Number(seconds) * 1000); return Number.isFinite(date.getTime()) ? date.toISOString() : null; }
async function fetchCoinGecko<T>(path: string, params: Record<string, string>, acquisitionMode: ProviderAcquisitionMode): Promise<T> { const url = new URL(`${COINGECKO_BASE}${path}`); Object.entries(params).forEach(([key, value]) => url.searchParams.set(key, value)); const apiKey = process.env.COINGECKO_API_KEY; const headers: Record<string, string> = { accept: "application/json" }; if (apiKey) headers["x-cg-demo-api-key"] = apiKey; const res = await fetch(url.toString(), { headers, ...providerFetchPolicy(acquisitionMode, 300), signal: AbortSignal.timeout(10_000) }); if (!res.ok) throw new Error(`CoinGecko HTTP ${res.status}`); return (await res.json()) as T; }
export async function fetchCryptoMarketObservations(symbols: string[] = ["BTC", "ETH"], acquisitionMode: ProviderAcquisitionMode = "CACHED"): Promise<ProviderResult<CryptoMarketObservationInput>> { const requestedSymbols = symbols.filter((symbol) => symbol in ASSET_IDS) as Array<keyof typeof ASSET_IDS>; if (requestedSymbols.length === 0) return providerResult("coingecko", "EMPTY", [], "No supported crypto symbols requested"); try { const ids = requestedSymbols.map((symbol) => ASSET_IDS[symbol]).join(","); const [assets, global] = await Promise.all([fetchCoinGecko<CoinGeckoSimplePrice>("/simple/price", { ids, vs_currencies: "usd", include_market_cap: "true", include_24hr_vol: "true", include_24hr_change: "true", include_last_updated_at: "true" }, acquisitionMode), fetchCoinGecko<CoinGeckoGlobal>("/global", {}, acquisitionMode)]); const data: CryptoMarketObservationInput[] = []; for (const symbol of requestedSymbols) { const id = ASSET_IDS[symbol]; const item = assets[id]; const observedAt = observedAtFromUnix(item?.last_updated_at); if (!item || !observedAt) continue; const retrievedAt = new Date().toISOString(); const baseMetadata = { providerAssetId: id, quote: "USD", endpoint: "/simple/price" }; if (Number.isFinite(item.usd)) data.push({ metricId: `${symbol.toLowerCase()}.spot.usd`, symbol, value: Number(item.usd), observedAt, retrievedAt, source: "CoinGecko", metadata: { ...baseMetadata, metric: "spot_price", unit: "USD", changePct: Number.isFinite(item.usd_24h_change) ? Number(item.usd_24h_change) : null, changeBasis: "24h" } }); if (Number.isFinite(item.usd_market_cap)) data.push({ metricId: `${symbol.toLowerCase()}.market_cap.usd`, symbol, value: Number(item.usd_market_cap), observedAt, retrievedAt, source: "CoinGecko", metadata: { ...baseMetadata, metric: "market_cap", unit: "USD" } }); } const globalData = global.data; const globalObservedAt = observedAtFromUnix(globalData?.updated_at); if (globalData && globalObservedAt) { const retrievedAt = new Date().toISOString(); const globalMetrics: Array<{ metricId: string; symbol: string; value: number | undefined; metric: string; unit: string }> = [{ metricId: "crypto.total_market_cap.usd", symbol: "TOTAL_CRYPTO", value: globalData.total_market_cap?.usd, metric: "total_market_cap", unit: "USD" }, { metricId: "crypto.total_volume_24h.usd", symbol: "TOTAL_CRYPTO", value: globalData.total_volume?.usd, metric: "total_volume_24h", unit: "USD" }, { metricId: "crypto.btc_dominance.pct", symbol: "BTC", value: globalData.market_cap_percentage?.btc, metric: "btc_dominance", unit: "PERCENT" }, { metricId: "crypto.eth_dominance.pct", symbol: "ETH", value: globalData.market_cap_percentage?.eth, metric: "eth_dominance", unit: "PERCENT" }]; for (const metric of globalMetrics) { if (!Number.isFinite(metric.value)) continue; data.push({ metricId: metric.metricId, symbol: metric.symbol, value: Number(metric.value), observedAt: globalObservedAt, retrievedAt, source: "CoinGecko", metadata: { metric: metric.metric, unit: metric.unit, endpoint: "/global" } }); } } if (data.length === 0) return providerResult("coingecko", "EMPTY", [], "CoinGecko returned no canonical crypto observations"); return providerResult("coingecko", "SUCCESS", data); } catch (error) { return providerResult("coingecko", "ERROR", [], error instanceof Error ? error.message : "CoinGecko request failed"); } }
